// src/pages/admin/AdminTracking.jsx
// Shipment tracking — search and follow consignments via the
// configured courier partner (default: VRL). Pulled from the
// tracking tab inside the legacy Admin.jsx.
//
// Placeholder — implementation lands in a later step.

import React from 'react';

export default function AdminTracking() {
  return (
    <div className="admin-page" data-admin-page="tracking">
      <h1>Admin Tracking</h1>
      <p>Courier lookup and tracking results will appear here.</p>
    </div>
  );
}
